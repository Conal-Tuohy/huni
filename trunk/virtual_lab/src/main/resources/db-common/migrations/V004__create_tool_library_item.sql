CREATE TABLE "APP"."TOOL_LIBRARY_ITEM"(
ID BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY (START WITH 100),
NAME VARCHAR(64) not null,
UNIQUE (NAME),
DESCRIPTION VARCHAR(255),
URL VARCHAR(255) not null,
VERSION INTEGER)
